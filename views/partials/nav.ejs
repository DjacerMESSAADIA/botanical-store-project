<nav class="bg-green-800 text-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            <div class="flex items-center">
                <a href="/" class="flex items-center">
                    <span class="text-xl font-bold">Botanical Store</span>
                </a>
                <div class="ml-10 flex items-baseline space-x-4">
                    <a href="/" class="hover:bg-green-700 px-3 py-2 rounded-md">Home</a>
                    <a href="/products" class="hover:bg-green-700 px-3 py-2 rounded-md">Products</a>
                    <% if (user && user.isAdmin) { %>
                        <a href="/dashboard" class="hover:bg-green-700 px-3 py-2 rounded-md">Dashboard</a>
                    <% } %>
                </div>
            </div>
            <div class="flex items-center">
                <% if (user) { %>
                    <span class="mr-4">Welcome, <%= user.name %></span>
                    <a href="/logout" class="bg-green-700 hover:bg-green-600 px-4 py-2 rounded-md">Logout</a>
                <% } else { %>
                    <a href="/login" class="hover:bg-green-700 px-3 py-2 rounded-md">Login</a>
                    <a href="/register" class="bg-green-700 hover:bg-green-600 px-4 py-2 rounded-md ml-2">Register</a>
                <% } %>
            </div>
        </div>
    </div>
</nav> 